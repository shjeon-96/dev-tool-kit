import {
  createSeoPageMetadata,
  SeoContentPage,
} from "../_trend/seo-content-page";

export const metadata = createSeoPageMetadata("animal-speed-quiz");

export default function AnimalSpeedQuizPage() {
  return <SeoContentPage slug="animal-speed-quiz" />;
}
