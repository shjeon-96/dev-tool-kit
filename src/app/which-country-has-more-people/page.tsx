import {
  createSeoPageMetadata,
  SeoContentPage,
} from "../_trend/seo-content-page";

export const metadata = createSeoPageMetadata("which-country-has-more-people");

export default function WhichCountryHasMorePeoplePage() {
  return <SeoContentPage slug="which-country-has-more-people" />;
}
