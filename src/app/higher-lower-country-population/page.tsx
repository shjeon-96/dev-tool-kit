import {
  createSeoPageMetadata,
  SeoContentPage,
} from "../_trend/seo-content-page";

export const metadata = createSeoPageMetadata(
  "higher-lower-country-population",
);

export default function HigherLowerCountryPopulationPage() {
  return <SeoContentPage slug="higher-lower-country-population" />;
}
