import {
  createSeoPageMetadata,
  SeoContentPage,
} from "../_trend/seo-content-page";

export const metadata = createSeoPageMetadata("mountain-height-quiz");

export default function MountainHeightQuizPage() {
  return <SeoContentPage slug="mountain-height-quiz" />;
}
