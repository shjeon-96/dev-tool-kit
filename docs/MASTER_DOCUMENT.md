# DevToolkit 마스터 문서

> 모든 화면, 기능, 레이아웃에 대한 종합 가이드

**프로젝트명**: DevToolkit (Web Toolkit)
**버전**: 0.1.0
**프레임워크**: Next.js 14+ (App Router)
**스타일링**: Tailwind CSS 4
**UI**: Radix UI + Shadcn/ui 커스텀 컴포넌트
**다국어**: next-intl (영어, 한국어, 일본어)
**배포**: Vercel

---

## 목차

1. [애플리케이션 구조](#1-애플리케이션-구조)
2. [라우트 맵](#2-라우트-맵)
3. [레이아웃 시스템](#3-레이아웃-시스템)
4. [도구 카탈로그](#4-도구-카탈로그-31개)
5. [위젯 컴포넌트](#5-위젯-컴포넌트)
6. [공유 UI 컴포넌트](#6-공유-ui-컴포넌트)
7. [데이터 모델](#7-데이터-모델)
8. [UI 패턴](#8-ui-패턴)
9. [네비게이션 플로우](#9-네비게이션-플로우)
10. [반응형 디자인](#10-반응형-디자인)
11. [접근성](#11-접근성)
12. [SEO 및 메타데이터](#12-seo-및-메타데이터)
13. [성능 최적화](#13-성능-최적화)
14. [분석 및 모니터링](#14-분석-및-모니터링)

---

## 1. 애플리케이션 구조

### 1.1 디렉토리 구조

```
src/
├── app/                    # Next.js App Router
│   ├── [locale]/          # 다국어 라우트
│   │   ├── tools/         # 도구 페이지
│   │   ├── cheatsheets/   # 치트시트 페이지
│   │   ├── guides/        # 가이드 페이지
│   │   ├── privacy/       # 개인정보처리방침
│   │   └── layout.tsx     # 메인 레이아웃
│   ├── api/               # API 라우트
│   └── globals.css        # 전역 스타일
│
├── entities/              # 데이터 모델
│   ├── tool/              # 도구 엔티티
│   ├── cheatsheet/        # 치트시트 엔티티
│   └── guide/             # 가이드 엔티티
│
├── features/              # 도구 기능 컴포넌트
│   ├── json-formatter/
│   ├── jwt-decoder/
│   └── ... (31개 도구)
│
├── widgets/               # 레이아웃 위젯
│   ├── header/
│   ├── sidebar/
│   ├── footer/
│   ├── command-menu/
│   └── tools-list/
│
└── shared/                # 공유 리소스
    ├── ui/                # UI 컴포넌트
    ├── lib/               # 유틸리티
    └── config/            # 설정
```

### 1.2 아키텍처 패턴

- **Feature-Sliced Design (FSD)**: 기능별 모듈화
- **Server Components**: 기본 서버 렌더링
- **Client Components**: 상호작용이 필요한 UI만 클라이언트
- **Dynamic Import**: 도구 컴포넌트 지연 로딩

---

## 2. 라우트 맵

### 2.1 라우트 트리

```
/                                    # 루트 (리다이렉트)
├── /[locale]                        # 로케일 루트
│   ├── /                            # 홈 → /tools 리다이렉트
│   │
│   ├── /tools                       # 도구 목록 페이지
│   │   └── /[slug]                  # 개별 도구 상세
│   │       ├── json-formatter
│   │       ├── jwt-decoder
│   │       └── ... (31개)
│   │
│   ├── /cheatsheets                 # 치트시트 목록
│   │   └── /[slug]                  # 개별 치트시트
│   │       ├── git
│   │       ├── http-status
│   │       └── ... (14개)
│   │
│   ├── /guides                      # 가이드 목록
│   │   └── /[slug]                  # 개별 가이드
│   │
│   └── /privacy                     # 개인정보처리방침
│
├── /api
│   └── /og                          # OG 이미지 동적 생성
│
├── /robots.txt                      # 검색엔진 로봇
└── /sitemap.xml                     # 사이트맵
```

### 2.2 페이지별 설명

| 페이지               | 경로                           | 설명                                 |
| -------------------- | ------------------------------ | ------------------------------------ |
| **도구 목록**        | `/[locale]/tools`              | 31개 도구를 카테고리별로 그리드 표시 |
| **도구 상세**        | `/[locale]/tools/[slug]`       | 개별 도구 UI 및 SEO 콘텐츠           |
| **치트시트 목록**    | `/[locale]/cheatsheets`        | 14개 치트시트 그리드                 |
| **치트시트 상세**    | `/[locale]/cheatsheets/[slug]` | 검색 가능한 참조 테이블              |
| **가이드 목록**      | `/[locale]/guides`             | 도구별 사용 가이드                   |
| **가이드 상세**      | `/[locale]/guides/[slug]`      | 상세 튜토리얼                        |
| **개인정보처리방침** | `/[locale]/privacy`            | 법적 고지                            |

---

## 3. 레이아웃 시스템

### 3.1 메인 레이아웃 구조

```tsx
// src/app/[locale]/layout.tsx
<html lang={locale}>
  <body>
    <div className="flex h-screen bg-background">
      {/* 사이드바 - 모바일에서 숨김 */}
      <aside className="hidden w-64 flex-col md:flex">
        <Sidebar />
      </aside>

      {/* 메인 콘텐츠 영역 */}
      <main className="flex-1 flex flex-col overflow-hidden">
        <Header /> {/* 고정 헤더 */}
        <div className="flex-1 overflow-y-auto p-6">
          {children} {/* 페이지 콘텐츠 */}
        </div>
        <Footer /> {/* 고정 푸터 */}
      </main>
    </div>
  </body>
</html>
```

### 3.2 레이아웃 특징

| 구성요소     | 데스크톱    | 모바일        |
| ------------ | ----------- | ------------- |
| **사이드바** | 256px 고정  | 드로어로 숨김 |
| **헤더**     | 전체 너비   | 전체 너비     |
| **콘텐츠**   | 스크롤 가능 | 스크롤 가능   |
| **푸터**     | 하단 고정   | 하단 고정     |

### 3.3 페이지 레이아웃 패턴

#### 도구 목록 페이지

```
┌─────────────────────────────────────┐
│  DevToolkit                         │
│  모든 개발자 도구                    │
├─────────────────────────────────────┤
│  ⭐ 즐겨찾기 (있을 경우)             │
│  ┌────┐ ┌────┐ ┌────┐ ┌────┐       │
│  │Tool│ │Tool│ │Tool│ │Tool│       │
│  └────┘ └────┘ └────┘ └────┘       │
├─────────────────────────────────────┤
│  🕐 최근 사용 (있을 경우)            │
│  ┌────┐ ┌────┐ ┌────┐              │
│  │Tool│ │Tool│ │Tool│              │
│  └────┘ └────┘ └────┘              │
├─────────────────────────────────────┤
│  📝 텍스트 & 코드                   │
│  ┌────┐ ┌────┐ ┌────┐              │
│  │JSON│ │JWT │ │UUID│              │
│  └────┘ └────┘ └────┘              │
├─────────────────────────────────────┤
│  🎨 미디어 & 디자인                  │
│  ┌────┐ ┌────┐ ┌────┐              │
│  │Img │ │QR  │ │Icon│              │
│  └────┘ └────┘ └────┘              │
└─────────────────────────────────────┘
```

#### 도구 상세 페이지

```
┌─────────────────────────────────────┐
│  [Icon] JSON Formatter        [⭐]  │
│         JSON 포맷터, 검증기          │
├─────────────────────────────────────┤
│  ┌─────────────────────────────────┐│
│  │                                 ││
│  │     [도구 UI 컴포넌트]           ││
│  │                                 ││
│  │     입력/출력 패널               ││
│  │     옵션 컨트롤                  ││
│  │     액션 버튼                    ││
│  │                                 ││
│  └─────────────────────────────────┘│
├─────────────────────────────────────┤
│  📖 이 도구는 무엇인가요?            │
│  JSON Formatter는 JSON 데이터를...  │
│                                     │
│  📝 사용 방법                        │
│  1. 왼쪽 입력창에 JSON을...         │
│                                     │
│  ❓ 자주 묻는 질문                   │
│  ▸ 입력한 데이터가 서버에 저장되나요? │
│  ▸ 대용량 파일도 처리할 수 있나요?   │
└─────────────────────────────────────┘
```

---

## 4. 도구 카탈로그 (31개)

### 4.1 카테고리별 분류

#### 📝 텍스트 & 코드 (16개)

| 도구                | Slug                  | 설명                                   |
| ------------------- | --------------------- | -------------------------------------- |
| JSON Formatter      | `json-formatter`      | JSON 포맷팅, 검증, 압축                |
| JWT Decoder         | `jwt-decoder`         | JWT 토큰 디코딩 (헤더, 페이로드, 만료) |
| UUID Generator      | `uuid-generator`      | UUID v1/v4, ULID 생성                  |
| SQL Formatter       | `sql-formatter`       | SQL 쿼리 포맷팅 (다중 방언 지원)       |
| Cron Parser         | `cron-parser`         | Cron 표현식 파싱 및 실행 예측          |
| Markdown Preview    | `markdown-preview`    | 실시간 마크다운 렌더링                 |
| Diff Checker        | `diff-checker`        | 텍스트 비교 및 차이점 시각화           |
| Lorem Generator     | `lorem-generator`     | 더미 텍스트 생성                       |
| URL Encoder         | `url-encoder`         | URL 인코딩/디코딩                      |
| URL Parser          | `url-parser`          | URL 파싱 및 쿼리 파라미터 편집         |
| UA Parser           | `ua-parser`           | User Agent 문자열 파싱                 |
| Regex Tester        | `regex-tester`        | 정규식 테스트 및 매칭                  |
| Meta Generator      | `meta-generator`      | SEO 메타 태그, OG, Twitter Card 생성   |
| cURL Builder        | `curl-builder`        | HTTP 요청 빌더 (cURL 출력)             |
| Prettier Playground | `prettier-playground` | 다중 언어 코드 포맷팅                  |
| Text Case Converter | `text-case-converter` | 텍스트 케이스 변환                     |

#### 🎨 미디어 & 디자인 (7개)

| 도구               | Slug                 | 설명                                     |
| ------------------ | -------------------- | ---------------------------------------- |
| Image Resizer      | `image-resizer`      | 이미지 리사이즈, 포맷 변환, 품질 조절    |
| App Icon Generator | `app-icon-generator` | iOS, Android, Favicon 에셋 생성          |
| QR Generator       | `qr-generator`       | QR 코드 생성 (URL, WiFi, 연락처, 텍스트) |
| Color Picker       | `color-picker`       | 이미지에서 색상 추출, 팔레트 생성        |
| Box Shadow         | `box-shadow`         | CSS box-shadow 비주얼 에디터             |
| Gradient Generator | `gradient-generator` | CSS 그라디언트 비주얼 에디터             |
| SVG Optimizer      | `svg-optimizer`      | SVG 파일 최적화 및 크기 감소             |

#### 🔄 변환 도구 (6개)

| 도구               | Slug                 | 설명                              |
| ------------------ | -------------------- | --------------------------------- |
| Unix Timestamp     | `unix-timestamp`     | Unix 타임스탬프 ↔ 날짜/시간 변환  |
| Base64 Converter   | `base64-converter`   | 텍스트/파일 Base64 인코딩/디코딩  |
| Base Converter     | `base-converter`     | 2진수, 8진수, 10진수, 16진수 변환 |
| HTML Entity        | `html-entity`        | HTML 특수문자 인코딩/디코딩       |
| CSS to Tailwind    | `css-to-tailwind`    | CSS → Tailwind CSS 클래스 변환    |
| JSON to TypeScript | `json-to-typescript` | JSON → TypeScript 인터페이스/타입 |

#### 🔒 보안 (2개)

| 도구           | Slug             | 설명                                     |
| -------------- | ---------------- | ---------------------------------------- |
| Hash Generator | `hash-generator` | 해시 생성 (MD5, SHA-1, SHA-256, SHA-512) |
| CSS Minifier   | `css-minifier`   | CSS 코드 압축 및 최적화                  |

### 4.2 도구 컴포넌트 구조

```
src/features/[tool-name]/
├── index.ts                    # Export 선언
├── ui/
│   └── [tool-name].tsx        # 메인 UI 컴포넌트
├── model/
│   └── use-[tool-name].ts     # 커스텀 훅 (상태/로직)
└── lib/
    └── [utils].ts             # 유틸리티 함수
```

### 4.3 공통 도구 기능

모든 도구에서 공통으로 제공되는 기능:

- **입력/출력 패널**: 좌우 분할 또는 상하 분할
- **복사 버튼**: 결과를 클립보드에 복사
- **초기화 버튼**: 입력/출력 초기화
- **붙여넣기 버튼**: 클립보드에서 붙여넣기
- **히스토리 패널**: 최근 작업 기록 (일부 도구)
- **공유 버튼**: URL로 상태 공유 (일부 도구)
- **즐겨찾기**: 별표로 즐겨찾기 추가/제거

---

## 5. 위젯 컴포넌트

### 5.1 Header (`src/widgets/header/`)

```
┌────────────────────────────────────────────────────────┐
│ [☰]  [🔍 Search tools...  ⌘K]  [🌐]  [🌙/☀️]          │
└────────────────────────────────────────────────────────┘
  │          │                    │       │
  │          │                    │       └─ 테마 토글
  │          │                    └─ 언어 전환
  │          └─ 커맨드 팔레트 (⌘K)
  └─ 모바일 메뉴 토글
```

**구성 요소:**

- **MobileMenuButton**: 모바일에서만 표시, 사이드바 드로어 열기
- **CommandMenu**: 도구 검색 및 빠른 탐색 (⌘K / Ctrl+K)
- **LanguageSwitcher**: 언어 전환 드롭다운 (en, ko, ja)
- **ModeToggle**: 라이트/다크 모드 토글

### 5.2 Sidebar (`src/widgets/sidebar/`)

```
┌──────────────────────┐
│    DevToolkit        │
├──────────────────────┤
│ [📋] All Tools       │
├──────────────────────┤
│ TEXT & CODE          │
│   ├─ JSON Formatter  │
│   ├─ JWT Decoder     │
│   └─ ...             │
├──────────────────────┤
│ MEDIA & DESIGN       │
│   ├─ Image Resizer   │
│   ├─ QR Generator    │
│   └─ ...             │
├──────────────────────┤
│ CONVERTERS           │
│   ├─ Base64          │
│   ├─ Unix Timestamp  │
│   └─ ...             │
├──────────────────────┤
│ SECURITY             │
│   ├─ Hash Generator  │
│   └─ JWT Decoder     │
└──────────────────────┘
```

**기능:**

- 카테고리별 그룹화된 도구 목록
- 현재 페이지 활성화 표시
- 툴팁 및 아이콘
- 스크롤 가능한 컨테이너

### 5.3 Footer (`src/widgets/footer/`)

```
┌────────────────────────────────────────────────────────┐
│ © 2025 Web Toolkit          개인정보처리방침 | GitHub   │
└────────────────────────────────────────────────────────┘
```

### 5.4 CommandMenu (`src/widgets/command-menu/`)

**단축키:** ⌘K (Mac) / Ctrl+K (Windows/Linux)

```
┌─────────────────────────────────────┐
│ 🔍 Search tools...                  │
├─────────────────────────────────────┤
│ TOOLS                               │
│   [JSON] JSON Formatter             │
│   [🔐] JWT Decoder                  │
│   [#] UUID Generator                │
│   ...                               │
└─────────────────────────────────────┘
```

### 5.5 FavoriteRecentSection (`src/widgets/tools-list/`)

```
┌─────────────────────────────────────┐
│ ⭐ Favorites                        │
│ ┌────┐ ┌────┐ ┌────┐ ┌────┐       │
│ │Tool│ │Tool│ │Tool│ │Tool│       │
│ │ ❌ │ │ ❌ │ │ ❌ │ │ ❌ │       │
│ └────┘ └────┘ └────┘ └────┘       │
├─────────────────────────────────────┤
│ 🕐 Recent                    Clear  │
│ ┌────┐ ┌────┐ ┌────┐              │
│ │Tool│ │Tool│ │Tool│              │
│ └────┘ └────┘ └────┘              │
└─────────────────────────────────────┘
```

---

## 6. 공유 UI 컴포넌트

### 6.1 폼 컴포넌트

| 컴포넌트   | 파일           | 설명                                                 |
| ---------- | -------------- | ---------------------------------------------------- |
| `Button`   | `button.tsx`   | 버튼 (variant: default, outline, ghost, destructive) |
| `Input`    | `input.tsx`    | 텍스트 입력 필드                                     |
| `Textarea` | `textarea.tsx` | 멀티라인 텍스트 입력                                 |
| `Label`    | `label.tsx`    | 폼 필드 레이블                                       |
| `Select`   | `select.tsx`   | 드롭다운 선택                                        |
| `Slider`   | `slider.tsx`   | 범위 슬라이더                                        |
| `Switch`   | `switch.tsx`   | 토글 스위치                                          |
| `Tabs`     | `tabs.tsx`     | 탭 인터페이스                                        |

### 6.2 다이얼로그 & 메뉴

| 컴포넌트       | 파일                | 설명            |
| -------------- | ------------------- | --------------- |
| `Dialog`       | `dialog.tsx`        | 모달 다이얼로그 |
| `Sheet`        | `sheet.tsx`         | 사이드 드로어   |
| `DropdownMenu` | `dropdown-menu.tsx` | 드롭다운 메뉴   |
| `Command`      | `command.tsx`       | 커맨드 팔레트   |

### 6.3 피드백 컴포넌트

| 컴포넌트         | 파일                | 설명            |
| ---------------- | ------------------- | --------------- |
| `Spinner`        | `spinner.tsx`       | 로딩 스피너     |
| `LoadingOverlay` | `spinner.tsx`       | 로딩 오버레이   |
| `Skeleton`       | `skeleton.tsx`      | 스켈레톤 로딩   |
| `SkeletonText`   | `skeleton.tsx`      | 텍스트 스켈레톤 |
| `SkeletonCard`   | `skeleton.tsx`      | 카드 스켈레톤   |
| `ErrorMessage`   | `error-message.tsx` | 에러 메시지     |
| `EmptyState`     | `error-message.tsx` | 빈 상태 표시    |

### 6.4 SEO 컴포넌트

| 컴포넌트           | 파일          | 설명              |
| ------------------ | ------------- | ----------------- |
| `JsonLd`           | `json-ld.tsx` | 기본 JSON-LD      |
| `BreadcrumbJsonLd` | `json-ld.tsx` | 브레드크럼 스키마 |
| `ProductJsonLd`    | `json-ld.tsx` | 제품 스키마       |
| `FaqJsonLd`        | `json-ld.tsx` | FAQ 스키마        |
| `HowToJsonLd`      | `json-ld.tsx` | HowTo 스키마      |
| `ArticleJsonLd`    | `json-ld.tsx` | Article 스키마    |

### 6.5 기타 컴포넌트

| 컴포넌트        | 파일                 | 설명                       |
| --------------- | -------------------- | -------------------------- |
| `ShareButton`   | `share-button.tsx`   | 공유 버튼                  |
| `ThemeProvider` | `theme-provider.tsx` | 테마 제공자                |
| `ClarityScript` | `clarity.tsx`        | Microsoft Clarity 스크립트 |
| `AdUnit`        | `ad-unit/`           | Google AdSense 광고        |

---

## 7. 데이터 모델

### 7.1 Tool Entity

```typescript
// src/entities/tool/model/types.ts
type ToolCategory = "text" | "media" | "security" | "converters";

type ToolSlug =
  | "json-formatter"
  | "jwt-decoder"
  | "uuid-generator"
  | "image-resizer"
  | "qr-generator"
  | "hash-generator";
// ... 31개 도구

interface Tool {
  title: string; // 도구 제목
  description: string; // 도구 설명
  icon: LucideIcon; // Lucide 아이콘
  category: ToolCategory; // 카테고리
}
```

### 7.2 Cheatsheet Entity

```typescript
// src/entities/cheatsheet/model/types.ts
interface CheatsheetItem {
  code: string; // 코드/명령어
  name: string; // 이름
  description: string; // 설명
  example?: string; // 예시
  category?: string; // 카테고리
}

interface Cheatsheet {
  slug: string;
  title: string;
  description: string;
  items: CheatsheetItem[];
}
```

### 7.3 Guide Entity

```typescript
// src/entities/guide/model/types.ts
interface GuideSection {
  id: string;
  title: string;
  content: string;
  code?: string;
  language?: string;
}

interface Guide {
  slug: string;
  sections: GuideSection[];
  relatedTools: string[];
  keywords: string[];
  difficulty: "beginner" | "intermediate" | "advanced";
  readTime: number;
}
```

---

## 8. UI 패턴

### 8.1 카드 컴포넌트 패턴

```tsx
// 도구 카드
<Link href={`/${locale}/tools/${slug}`}>
  <div className="rounded-lg border p-6 hover:border-primary/50 hover:shadow-md">
    <div className="flex items-center gap-4">
      <div className="flex h-12 w-12 items-center justify-center rounded-lg bg-muted">
        <Icon className="h-6 w-6" />
      </div>
      <div>
        <h3 className="font-semibold">{title}</h3>
        <p className="text-sm text-muted-foreground line-clamp-2">
          {description}
        </p>
      </div>
    </div>
  </div>
</Link>
```

### 8.2 입력/출력 패널 패턴

```tsx
// 2열 그리드 레이아웃
<div className="grid gap-4 md:grid-cols-2">
  {/* 입력 패널 */}
  <div className="space-y-2">
    <label htmlFor="input" className="text-sm font-medium">
      Input
    </label>
    <textarea
      id="input"
      className="h-[400px] w-full rounded-md border bg-muted/50 p-3 font-mono text-sm"
      value={input}
      onChange={(e) => setInput(e.target.value)}
    />
  </div>

  {/* 출력 패널 */}
  <div className="space-y-2">
    <div className="flex items-center justify-between">
      <label htmlFor="output" className="text-sm font-medium">
        Output
      </label>
      <Button variant="ghost" size="sm" onClick={handleCopy}>
        <Copy className="h-4 w-4" />
        Copy
      </Button>
    </div>
    <textarea
      id="output"
      className="h-[400px] w-full rounded-md border bg-muted/50 p-3 font-mono text-sm"
      value={output}
      readOnly
    />
  </div>
</div>
```

### 8.3 에러 표시 패턴

```tsx
{
  error && (
    <div
      role="alert"
      aria-live="polite"
      className="rounded-md bg-destructive/10 border border-destructive/20 p-3 text-sm text-destructive"
    >
      <AlertTriangle className="h-4 w-4 mr-2" aria-hidden="true" />
      {error}
    </div>
  );
}
```

### 8.4 히스토리 패널 패턴

```tsx
{
  showHistory && hasHistory && (
    <div className="rounded-md border bg-muted/30 p-4">
      <div className="flex items-center justify-between mb-3">
        <h3 className="text-sm font-medium">Recent History</h3>
        <div className="flex gap-2">
          <Button variant="ghost" size="sm" onClick={clearHistory}>
            Clear All
          </Button>
          <Button
            variant="ghost"
            size="sm"
            onClick={() => setShowHistory(false)}
          >
            <X className="h-4 w-4" />
          </Button>
        </div>
      </div>
      <div className="space-y-2 max-h-48 overflow-y-auto">
        {history.map((item) => (
          <button
            key={item.id}
            onClick={() => loadFromHistory(item.input, item.output)}
            className="w-full text-left p-2 rounded-md hover:bg-muted"
          >
            <span className="font-mono text-xs">
              {truncateText(item.input)}
            </span>
            <span className="text-xs text-muted-foreground">
              {formatTimestamp(item.timestamp)}
            </span>
          </button>
        ))}
      </div>
    </div>
  );
}
```

---

## 9. 네비게이션 플로우

```
                        ┌─────────────┐
                        │   진입점    │
                        │     /       │
                        └──────┬──────┘
                               │
                               ▼
                    ┌──────────────────┐
                    │  [locale] 감지   │
                    └────────┬─────────┘
                             │
                             ▼
                    ┌──────────────────┐
                    │   /tools (홈)   │
                    └────────┬─────────┘
                             │
        ┌────────────────────┼────────────────────┐
        │                    │                    │
        ▼                    ▼                    ▼
┌───────────────┐   ┌───────────────┐   ┌───────────────┐
│ 도구 상세      │   │ 치트시트      │   │ 가이드        │
│ /tools/[slug] │   │ /cheatsheets  │   │ /guides       │
└───────────────┘   └───────┬───────┘   └───────┬───────┘
                            │                    │
                            ▼                    ▼
                    ┌───────────────┐   ┌───────────────┐
                    │ 치트시트 상세 │   │ 가이드 상세   │
                    │ /[slug]       │   │ /[slug]       │
                    └───────────────┘   └───────────────┘

사이드바 네비게이션 (항상 접근 가능)
├── "All Tools" 링크 → /tools
├── 카테고리별 도구 링크
│   ├── Text & Code (16개)
│   ├── Media & Design (7개)
│   ├── Converters (6개)
│   └── Security (2개)
└── 커맨드 팔레트 (⌘K) → 빠른 검색/탐색
```

---

## 10. 반응형 디자인

### 10.1 브레이크포인트

| 브레이크포인트    | 너비     | 레이아웃 변화                          |
| ----------------- | -------- | -------------------------------------- |
| **기본 (모바일)** | < 768px  | 1열 그리드, 사이드바 숨김, 드로어 사용 |
| **md (태블릿)**   | ≥ 768px  | 2열 그리드, 사이드바 표시              |
| **lg (데스크톱)** | ≥ 1024px | 3열 그리드, 전체 레이아웃              |

### 10.2 반응형 그리드

```css
/* 도구 목록 그리드 */
.grid.gap-4.md\:grid-cols-2.lg\:grid-cols-3

/* 입력/출력 그리드 */
.grid.gap-4.md\:grid-cols-2
```

### 10.3 모바일 최적화

- **터치 타겟**: 최소 44x44px (WCAG 2.1 AAA)
- **폰트 크기**: 입력 필드 16px (iOS 줌 방지)
- **간격**: 모바일에서 더 넓은 gap

---

## 11. 접근성

### 11.1 ARIA 속성

```tsx
// 버튼 접근성
<Button
  aria-label="Copy to clipboard"
  aria-pressed={copied}
>
  <Copy aria-hidden="true" />
</Button>

// 에러 메시지 접근성
<div role="alert" aria-live="polite">
  {error}
</div>

// 토글 패널 접근성
<Button
  aria-expanded={showHistory}
  aria-label="Toggle history panel"
>
  History
</Button>

// 폼 레이블 연결
<label htmlFor="json-input">Input</label>
<textarea
  id="json-input"
  aria-describedby={error ? "json-error" : undefined}
/>
```

### 11.2 키보드 네비게이션

- **Tab**: 포커스 이동
- **Enter/Space**: 버튼 활성화
- **⌘K / Ctrl+K**: 커맨드 팔레트
- **Escape**: 모달/드로어 닫기

### 11.3 색상 대비

- **다크모드**: muted-foreground 대비 개선 (0.75 oklch)
- **테두리**: 다크모드에서 15% → 18% 불투명도
- **플레이스홀더**: 충분한 대비 (0.6 oklch)

---

## 12. SEO 및 메타데이터

### 12.1 메타 태그 전략

```tsx
// 페이지별 동적 메타데이터
export async function generateMetadata({ params }: Props): Promise<Metadata> {
  const { locale, slug } = await params;
  const t = await getTranslations({ locale, namespace: "tools" });

  return {
    title: t(`${slug}.title`),
    description: t(`${slug}.description`),
    openGraph: {
      title,
      description,
      url: `/${locale}/tools/${slug}`,
      images: [ogImageUrl],
    },
    twitter: {
      card: "summary_large_image",
      title,
      description,
    },
    alternates: {
      canonical: `/${locale}/tools/${slug}`,
      languages: {
        en: `/en/tools/${slug}`,
        ko: `/ko/tools/${slug}`,
        ja: `/ja/tools/${slug}`,
      },
    },
  };
}
```

### 12.2 구조화된 데이터

```tsx
// 브레드크럼
<BreadcrumbJsonLd items={[
  { name: "Home", url: baseUrl },
  { name: "Tools", url: `${baseUrl}/tools` },
  { name: title, url: `${baseUrl}/tools/${slug}` },
]} />

// 제품 스키마
<ProductJsonLd
  name={title}
  description={description}
  url={productUrl}
  category={tool.category}
/>

// FAQ 스키마
<FaqJsonLd faqs={faqItems} />
```

---

## 13. 성능 최적화

### 13.1 코드 스플리팅

```tsx
// 동적 임포트로 도구 컴포넌트 지연 로딩
const toolComponents: Record<ToolSlug, ComponentType> = {
  "json-formatter": dynamic(
    () => import("@/features/json-formatter").then((m) => m.JsonFormatter),
    { ssr: false },
  ),
  // ... 31개 도구
};
```

### 13.2 캐싱 전략

| 저장소       | 키               | 데이터             |
| ------------ | ---------------- | ------------------ |
| localStorage | `tool-favorites` | 즐겨찾기 도구 목록 |
| localStorage | `tool-recent`    | 최근 사용 도구     |
| localStorage | `theme`          | 테마 설정          |
| URL params   | `data`           | 공유 상태 (압축)   |

### 13.3 이미지 최적화

- **아이콘**: Lucide React (SVG, Tree-shakeable)
- **앱 아이콘**: PNG (192x192, 512x512)
- **동적 OG**: `/api/og` 라우트

---

## 14. 분석 및 모니터링

### 14.1 분석 도구

| 서비스                 | 목적                |
| ---------------------- | ------------------- |
| **Google Analytics 4** | 트래픽, 이벤트 추적 |
| **Google Tag Manager** | 태그 관리           |
| **Microsoft Clarity**  | 히트맵, 세션 녹화   |
| **Google AdSense**     | 광고 수익화         |

### 14.2 데이터 수집 정책

- **클라이언트 사이드만**: 서버로 데이터 전송 없음
- **개인정보 보호**: 모든 도구 처리는 브라우저 내에서
- **쿠키**: Google AdSense 맞춤 광고용

---

## 통계 요약

| 항목                      | 수량                                           |
| ------------------------- | ---------------------------------------------- |
| **총 도구**               | 31개                                           |
| **치트시트**              | 14개                                           |
| **가이드**                | 28개+                                          |
| **UI 컴포넌트**           | 25+                                            |
| **지원 언어**             | 3개 (en, ko, ja)                               |
| **라우트 카테고리**       | 5개 (Tools, Cheatsheets, Guides, Privacy, API) |
| **반응형 브레이크포인트** | 3개 (mobile, tablet, desktop)                  |

---

_최종 업데이트: 2025년 12월_
