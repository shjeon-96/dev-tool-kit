# Converters & Formatters (변환 및 포맷팅)

## 🎯 카테고리 개요

**목적**: 개발자들이 가장 자주 사용하는 데이터 변환 및 포맷팅 도구 제공

**타겟 사용자**:
- 프론트엔드/백엔드 개발자
- 데이터 엔지니어
- API 개발자
- 기술 문서 작성자

**핵심 가치**:
- 즉각적인 결과 확인
- 다양한 포맷 지원
- 실시간 검증
- 일관된 사용 경험

---

## 🛠️ 도구별 상세 명세

### 1. JSON Formatter

#### 기능 상세
```
입력 → 파싱 → 검증 → 포맷팅 → 출력
```

**주요 기능**:
- **Beautify**: 들여쓰기 레벨 조절 (2, 4 spaces, tabs)
- **Minify**: 모든 공백 제거 (한 줄 출력)
- **Validate**: 실시간 구문 오류 표시
- **Convert**: JSON ↔ YAML ↔ XML 상호 변환
- **Tree View**: 접을 수 있는 트리 구조 표시
- **Path Finder**: JSONPath로 특정 값 찾기
- **Schema Validation**: JSON Schema 검증 (고급)

#### UI/UX 설계
```
┌─────────────────────────────────────────────┐
│ [Beautify] [Minify] [Validate] [Tree View]  │
├─────────────────┬───────────────────────────┤
│                 │                           │
│   Input Editor  │   Output Viewer          │
│   (Monaco)      │   (Monaco/Tree)          │
│                 │                           │
│ Error: Line 5   │   ✓ Valid JSON           │
└─────────────────┴───────────────────────────┘
         [Copy] [Download] [Clear]
```

#### 구현 고려사항
- **성능**: 대용량 JSON (>10MB) 처리 시 Web Worker 사용
- **에러 처리**: 친화적인 에러 메시지와 위치 표시
- **단축키**: Cmd/Ctrl + Enter로 포맷팅
- **히스토리**: 최근 5개 작업 Undo/Redo

#### SEO 키워드
- Primary: "json formatter", "json beautifier", "json validator"
- Secondary: "format json online", "json pretty print", "json minify"
- Long-tail: "json formatter with validation", "json to yaml converter online"

---

### 2. SQL Formatter

#### 기능 상세
**SQL 방언 지원**:
- MySQL
- PostgreSQL
- SQL Server
- Oracle
- SQLite
- MariaDB

**포맷팅 옵션**:
- **대소문자**: keywords (UPPER/lower/Capitalize)
- **들여쓰기**: 2/4 spaces, tabs
- **줄바꿈**: SELECT 항목별, WHERE 조건별
- **정렬**: 테이블 별칭, 컬럼 정렬

#### UI/UX 설계
```
┌─────────────────────────────────────────────┐
│ Dialect: [MySQL ▼]  Case: [UPPER ▼]        │
├─────────────────┬───────────────────────────┤
│ SELECT * FROM   │ SELECT                    │
│ users WHERE id  │     *                     │
│ = 1             │ FROM                      │
│                 │     users                 │
│                 │ WHERE                     │
│                 │     id = 1                │
└─────────────────┴───────────────────────────┘
```

#### 구현 고려사항
- **구문 하이라이팅**: 방언별 키워드 인식
- **자동 완성**: 테이블/컬럼명 제안 (선택사항)
- **실행 계획**: EXPLAIN 결과 시각화 (고급)

---

### 3. Markdown Preview

#### 기능 상세
**렌더링 엔진**:
- GitHub Flavored Markdown (GFM)
- CommonMark
- 확장 문법 (표, 각주, 수식)

**테마 옵션**:
- GitHub
- VS Code
- Bear
- Custom CSS

**내보내기**:
- HTML (styled)
- PDF (via print)
- Markdown (formatted)

#### UI/UX 설계
```
┌────────────────┬────────────────────┐
│ # Markdown     │ Markdown           │
│ - Editor       │ • Editor           │
│ - Live Preview │ • Live Preview     │
│                │                    │
│ Split: [50/50] │ Theme: [GitHub ▼]  │
└────────────────┴────────────────────┘
```

#### 구현 고려사항
- **스크롤 동기화**: 편집기-미리보기 동기화
- **이미지 처리**: 드래그앤드롭, 자동 업로드
- **플러그인**: Mermaid, KaTeX 지원

---

### 4. Diff Checker

#### 기능 상세
**비교 모드**:
- Side-by-side (좌우 비교)
- Inline (통합 보기)
- Git style (unified diff)

**비교 옵션**:
- 공백 무시
- 대소문자 무시
- 줄 끝 공백 무시

**언어 지원**:
- 자동 언어 감지
- 100+ 프로그래밍 언어
- 구문 하이라이팅

#### UI/UX 설계
```
┌─────────────────┬─────────────────┬───────┐
│ Original        │ Modified        │ Stats │
├─────────────────┼─────────────────┤       │
│ - old line      │ + new line      │ +5    │
│   same line     │   same line     │ -3    │
│ - removed       │                 │ ~2    │
└─────────────────┴─────────────────┴───────┘
```

#### 구현 고려사항
- **성능**: 대용량 파일 diff 시 가상 스크롤
- **병합**: 3-way merge 지원 (고급)
- **패치 생성**: diff를 patch 파일로 내보내기

---

### 5. Prettier Playground

#### 기능 상세
**지원 언어**:
- JavaScript/TypeScript
- CSS/SCSS/Less
- HTML/Vue/Angular
- JSON/YAML
- Markdown
- GraphQL

**설정 옵션**:
- Tab Width
- Use Tabs
- Semi colons
- Single Quotes
- Trailing Comma
- Bracket Spacing
- 기타 20+ 옵션

#### UI/UX 설계
```
┌──────────────┬──────────────┬──────────────┐
│ Input Code   │ Formatted    │ Options      │
│              │ Output       │ ┌──────────┐ │
│              │              │ │Tab: [2▼] │ │
│              │              │ │Semi: [✓] │ │
│              │              │ └──────────┘ │
└──────────────┴──────────────┴──────────────┘
                [Copy Config]
```

#### 구현 고려사항
- **버전 관리**: Prettier 버전 선택
- **설정 공유**: URL로 설정 공유
- **.prettierrc 생성**: 설정을 파일로 내보내기

---

## 📊 카테고리 전체 메트릭스

### 예상 사용 빈도 (일일)
1. JSON Formatter: 5,000+ 사용
2. SQL Formatter: 1,000+ 사용
3. Markdown Preview: 800+ 사용
4. Diff Checker: 600+ 사용
5. Prettier Playground: 400+ 사용

### 구현 복잡도
1. JSON Formatter: ⭐⭐ (중간)
2. SQL Formatter: ⭐⭐ (중간)
3. Markdown Preview: ⭐⭐⭐ (복잡)
4. Diff Checker: ⭐⭐⭐ (복잡)
5. Prettier Playground: ⭐⭐⭐⭐ (매우 복잡)

### 번들 사이즈 영향
1. JSON Formatter: ~150KB
2. SQL Formatter: ~80KB
3. Markdown Preview: ~200KB
4. Diff Checker: Monaco에 포함
5. Prettier Playground: ~500KB

---

## 🔧 공통 구현 패턴

### 1. 에디터 통합
```typescript
// 모든 코드 기반 도구는 Monaco Editor 사용
const editorConfig = {
  theme: 'vs-dark',
  minimap: { enabled: false },
  scrollBeyondLastLine: false,
  automaticLayout: true
};
```

### 2. 실시간 처리
```typescript
// Debounce로 성능 최적화
const debouncedFormat = useMemo(
  () => debounce(formatCode, 300),
  []
);
```

### 3. 에러 처리
```typescript
// 사용자 친화적 에러 메시지
interface FormatError {
  line: number;
  column: number;
  message: string;
  severity: 'error' | 'warning';
}
```

### 4. 결과 액션
```typescript
// 모든 도구 공통 액션
const commonActions = [
  { label: 'Copy', icon: Copy, shortcut: 'Cmd+C' },
  { label: 'Download', icon: Download },
  { label: 'Share', icon: Share2 },
  { label: 'Clear', icon: Trash2 }
];
```

---

## 🎨 UI 일관성 가이드

### 레이아웃 패턴
1. **2-Column**: Input/Output (JSON, SQL)
2. **Split View**: Editor/Preview (Markdown)
3. **3-Column**: Original/Modified/Stats (Diff)
4. **Tabbed**: Multiple views (Prettier)

### 컬러 스킴
- **Success**: `hsl(142, 71%, 45%)`
- **Error**: `hsl(0, 84%, 60%)`
- **Warning**: `hsl(38, 92%, 50%)`
- **Info**: `hsl(217, 91%, 60%)`

### 애니메이션
- **전환**: 300ms ease-in-out
- **로딩**: Skeleton screens
- **피드백**: Subtle bounce/fade

---

## 🚀 향후 확장 계획

### Phase 1 확장
- JSON: CSV 변환 추가
- SQL: 실행 계획 시각화

### Phase 2 확장
- GraphQL Formatter
- XML Formatter
- TOML/INI Formatter

### Phase 3 확장
- Code Translator (언어 간 변환)
- API Response Formatter
- Log Formatter